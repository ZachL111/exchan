# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its distributed data focus without claiming live deployment or external usage.

## Cases

- `baseline`: `clock drift`, score 197, lane `ship`
- `stress`: `join pressure`, score 152, lane `ship`
- `edge`: `presence churn`, score 189, lane `ship`
- `recovery`: `merge risk`, score 178, lane `ship`
- `stale`: `clock drift`, score 180, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
